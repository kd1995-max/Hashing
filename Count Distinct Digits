int countDistinct(int arr[] , int n)
{

	unordered_set<int> s;
	for(int i = 0; i<n; i++)
		s.insert(arr[i]);
	return s.size();
}

//shorter implementation(int arr[] , int n)
{
	unordered_set<int> s(arr , arr + n);
	return s.size();
}
